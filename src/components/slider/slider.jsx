import React, { useState, useEffect } from 'react';
import styles from './slider.module.scss';

const Slider = () => {
  return (
    <div className={styles.sliderContainer}>
      <h3 className="ml-5 mt-5">...Slider...</h3>
    </div>
  );
};

export default Slider;
